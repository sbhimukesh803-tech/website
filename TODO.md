# TODO

- [ ] Fix `ASTRA-AI/backend/package.json` scripts paths to correctly reference `server.js` from within the backend directory.
- [ ] Validate by running `npm run dev:backend` (from `ASTRA-AI/`) and checking that the server starts.
